{
  "name": "movconverter",
  "version": "0.1.1",
  "description": "Convert MOV to MP4 entirely in the browser — no server, no uploads. Lossless & instant remux for iPhone/Mac H.264/HEVC+AAC footage, PCM audio re-encoded via WebCodecs. Streaming, constant memory, 1GB+ files. TypeScript.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zikcheng/movconverter.git",
    "directory": "packages/core"
  },
  "homepage": "https://movtomp4.net",
  "bugs": {
    "url": "https://github.com/zikcheng/movconverter/issues"
  },
  "keywords": [
    "mov",
    "mp4",
    "video",
    "converter",
    "remux",
    "browser",
    "webcodecs",
    "quicktime"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./worker": {
      "types": "./dist/worker.d.ts",
      "default": "./dist/worker.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm build && pnpm test && pnpm typecheck"
  },
  "engines": {
    "node": ">=18"
  }
}
